# =============================================================================
# CDF Connection Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control

# =============================================================================
# REQUIRED: CDF Project Configuration
# =============================================================================
# CDF Project Name (required)
# You can use any of these variable names - they are all supported:
COGNITE_PROJECT=your-project-name
# Alternative names (commented out - uncomment if preferred):
# PROJECT=your-project-name
# CDF_PROJECT=your-project-name

# CDF Cluster (optional - used to construct base_url if base_url not provided)
# Format: cluster-name (e.g., "az-eastus-1", "europe-west1-1")
# This will construct: https://{cluster}.cognitedata.com
CDF_CLUSTER=az-eastus-1

# CDF Base URL (optional - defaults to https://api.cognite.com if not set)
# If CDF_CLUSTER is set, base_url will be constructed from it
# You can use any of these variable names:
COGNITE_BASE_URL=https://api.cognitedata.com
# Alternative names (commented out - uncomment if preferred):
# BASE_URL=https://api.cognitedata.com
# CDF_BASE_URL=https://api.cognitedata.com
# CDF_URL=https://api.cognitedata.com

# =============================================================================
# REQUIRED: Authentication - Choose ONE method
# =============================================================================

# -----------------------------------------------------------------------------
# Option 1: API Key Authentication (Simplest - Recommended for Development)
# -----------------------------------------------------------------------------
# Required for API Key authentication:
COGNITE_API_KEY=your-api-key-here
# Alternative names (commented out - uncomment if preferred):
# API_KEY=your-api-key-here
# CDF_API_KEY=your-api-key-here

# -----------------------------------------------------------------------------
# Option 2: OAuth Client Credentials (For Production/CDF Functions)
# -----------------------------------------------------------------------------
# Required for OAuth authentication (only if not using API Key):
#
# Using COGNITE_* prefix (recommended):
# COGNITE_TENANT_ID=your-tenant-id-here
# COGNITE_CLIENT_ID=your-client-id-here
# COGNITE_CLIENT_SECRET=your-client-secret-here
# COGNITE_TOKEN_URL=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/token
# COGNITE_SCOPES=https://{cluster}.cognitedata.com/.default
#
# OR using IDP_* prefix (also supported):
# IDP_TENANT_ID=your-tenant-id-here
# IDP_CLIENT_ID=your-client-id-here
# IDP_CLIENT_SECRET=your-client-secret-here
# IDP_TOKEN_URL=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/token
# IDP_SCOPES=https://{cluster}.cognitedata.com/.default
#
# OR using shorter aliases (also supported):
# TENANT_ID=your-tenant-id-here
# CLIENT_ID=your-client-id-here
# CLIENT_SECRET=your-client-secret-here
# TOKEN_URL=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/token
# SCOPES=https://{cluster}.cognitedata.com/.default
#
# Example OAuth configuration:
# COGNITE_TENANT_ID=dad80dc2-5b54-4059-837b-654b0d868fe5
# COGNITE_CLIENT_ID=5559bf66-178d-4e11-87c1-71e9f86664c2
# COGNITE_CLIENT_SECRET=your-secret-here
# COGNITE_TOKEN_URL=https://login.microsoftonline.com/dad80dc2-5b54-4059-837b-654b0d868fe5/oauth2/v2.0/token
# COGNITE_SCOPES=https://az-eastus-1.cognitedata.com/.default

# =============================================================================
# Optional: Data Model Configuration
# =============================================================================
# Default data model space for CDF views
# CDF_DATA_MODEL_SPACE=sp_enterprise_schema

# Default data model version
# CDF_DATA_MODEL_VERSION=v3

# =============================================================================
# Optional: Logging Configuration
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# =============================================================================
# Optional: Development/Debug Settings
# =============================================================================
# Enable debug mode for more verbose output
# DEBUG=false

# Enable verbose logging
# VERBOSE=false

# =============================================================================
# Notes
# =============================================================================
# 1. Authentication: You must provide EITHER API Key OR OAuth credentials, not both
# 2. Project Name: At least one project variable (COGNITE_PROJECT, PROJECT, or CDF_PROJECT) is required
# 3. Base URL: Will be auto-constructed from CDF_CLUSTER if not explicitly set
# 4. OAuth Scopes: Format is https://{cluster}.cognitedata.com/.default
# 5. For CDF Functions: OAuth is typically used instead of API keys
